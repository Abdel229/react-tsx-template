# Fichier modèle pour les variables d'environnement.
# Renommez ce fichier en .env et remplissez les valeurs appropriées.
# Les variables préfixées par VITE_ sont exposées au code client.

VITE_API_URL=http://localhost:8080/api
VITE_ANALYTICS_ID=votre_id_analytics_ici

# Exemple de variable non exposée au client (pour le backend ou le build)
# DB_CONNECTION_STRING=votre_chaine_de_connexion_db
